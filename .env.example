# modelmatch-frontend — environment template. Copy to .env (never commit .env).
# In production the API base URL is injected via a templated /config.js served by nginx;
# for local dev Vite reads VITE_-prefixed vars from .env.

VITE_API_BASE_URL=http://localhost:8000
